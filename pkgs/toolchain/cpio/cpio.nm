
include ../../core/cpio/cpio.nm

$(STAGE_BUILD): $(STAGE_PREPARE)
	@$(PRE_BUILD)

	cd $(DIR_APP) && \
		./configure \
			$(CONFIGURE_ARCH) \
			--prefix=$(TOOLS_DIR)

	cd $(DIR_APP) && make $(PARALLELISMFLAGS)

	@$(POST_BUILD)

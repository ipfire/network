
PKG_TOOLCHAIN_DEPS += gcc glibc ncurses

include ../../core/texinfo/texinfo.nm

define STAGE_BUILD
	cd $(DIR_APP) && \
		LDFLAGS="-lncursesw" \
		./configure \
			$(CONFIGURE_ARCH) \
			--prefix=$(TOOLS_DIR)

	cd $(DIR_APP) && make $(PARALLELISMFLAGS)
endef

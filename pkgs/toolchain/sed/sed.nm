
include ../../core/sed/sed.nm

define STAGE_BUILD
	cd $(DIR_APP) && \
		./configure \
			$(CONFIGURE_ARCH) \
			--prefix=$(TOOLS_DIR) \
			--without-included-regex

	cd $(DIR_APP) && make $(PARALLELISMFLAGS)
endef

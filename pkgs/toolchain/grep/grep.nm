
include ../../core/grep/grep.nm

define STAGE_BUILD
	cd $(DIR_APP) && \
		./configure \
			$(CONFIGURE_ARCH) \
			--prefix=$(TOOLS_DIR) \
			--disable-perl-regexp \
			--without-included-regex

	cd $(DIR_APP) && make $(PARALLELISMFLAGS)
endef

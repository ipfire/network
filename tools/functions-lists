#!/bin/bash

function listsort() {
	local item
	for item in $@; do
		echo "${item}"
	done | sort -u | tr "\n" " "
}

function listmatch() {
	local arg=${1}
	shift

	local item
	for item in $@; do
		if [ "${arg}" = "${item}" ]; then
			return 0
		fi
	done
	return 1
}

